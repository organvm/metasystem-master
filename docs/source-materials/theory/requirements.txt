# Metasystem Core Dependencies
# Python 3.10+

# Core dependencies
pyyaml>=6.0.3          # Configuration files
sqlalchemy>=2.0.51       # SQLite ORM (optional, for future)

# MCP Server
flask>=3.1.3           # HTTP server for MCP endpoints
flask-cors>=6.0.5      # CORS support

# File watching and monitoring
watchdog>=6.0.0        # File system monitoring for sorting daemon

# Optional: ML/AI for file classification
sentence-transformers>=5.6.0  # For semantic embeddings (optional)

# Utilities
python-magic>=0.4.27    # File type detection
requests>=2.34.2       # HTTP client
python-dateutil>=2.9.0.post0 # Date parsing

# Testing (Phase 9)
pytest>=9.1.1          # Test framework
pytest-cov>=7.1.0      # Coverage reporting
pytest-mock>=3.15.1    # Mocking support
pytest-benchmark>=5.2.3 # Performance benchmarks
freezegun>=1.5.5       # Time mocking
faker>=40.23.0          # Generate test data
coverage>=7.14.2        # Coverage measurement
coverage-badge>=1.1.2  # Coverage badges

# Test utilities
pytest-xdist>=3.8.0    # Parallel test execution
pytest-timeout>=2.4.0  # Prevent hanging tests
pytest-randomly>=4.1.0 # Randomize test order
